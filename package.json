{
  "name": "csc-307-group",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "format": "prettier --write './**/*.{js,jsx,ts,tsx,css,md,json}' --config ./.prettierrc.yaml",
    "start": "npm -w react-frontend run dev",
    "lint": "npx eslint . && npx prettier --check ."

  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "vite": "^5.4.11"
  }
}
